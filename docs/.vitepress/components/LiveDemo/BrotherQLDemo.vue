<script setup lang="ts">
// Placeholder — Phase 4 of the docs consolidation lifts the full
// brother-ql LiveDemo (647 lines) into this component, then refactors
// shared scaffolding (TextEditor, BitmapPreview, DevicePairButton,
// StatusPanel) out into ./shared/.
</script>

<template>
  <div class="live-demo-placeholder">
    <p><strong>Live demo coming soon.</strong></p>
    <p>
      The browser demo is being lifted into the unified docs site as part of
      Phase 4 of the docs consolidation. Until then, the brother-ql repo
      hosts the working demo on its archived gh-pages branch — see the
      <a href="/brother-ql/web">Web (WebUSB) guide</a> for the same code,
      runnable in any Chromium browser with a Brother QL printer.
    </p>
  </div>
</template>

<style scoped>
.live-demo-placeholder {
  padding: 1rem 1.25rem;
  border: 1px dashed var(--vp-c-divider);
  border-radius: 8px;
  background: var(--vp-c-bg-soft);
  margin: 1rem 0;
}
</style>
